<UserControl x:Class="Xeno.VsLinuxDebug.OptionsPages.RemoteDebuggerOptionsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="OptionsPageStyles.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>

  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel Style="{StaticResource PageRootStyle}">
      <GroupBox Header="Remote Debugger" Style="{StaticResource OptionsGroupBoxStyle}">
        <StackPanel>

          <StackPanel Style="{StaticResource FieldRowStyle}">
            <Label Content="Upload to folder" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding RemoteDeployBasePath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldTextBoxStyle}" />
            <TextBlock Style="{StaticResource FieldHelpTextStyle}"
                       Text="Folder for to transfer files to. For HOME folder, use './VSLinuxDbg' and not '~/VSLinuxDbg'" />
          </StackPanel>

          <StackPanel Style="{StaticResource FieldRowStyle}">
            <Label Content="Environment Variables" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding RemoteEnvironmentVariables, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FieldTextBoxStyle}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Height="90" />
            <TextBlock Style="{StaticResource FieldHelpTextStyle}"
                       Text="Environment variables passed to the debuggee, one 'KEY=VALUE' pair per line. Useful when the program reads required configuration from the environment (i.e. values normally supplied by systemd's EnvironmentFile)." />
          </StackPanel>

          <StackPanel Style="{StaticResource FieldRowStyle}">
            <Label Content="Service Name (optional)" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding RemoteServiceName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldTextBoxStyle}" />
            <TextBlock Style="{StaticResource FieldHelpTextStyle}"
                       Text="Name of a systemd unit (without '.service') that normally runs the debuggee, i.e. 'myapp' for 'myapp.service'. When set, it is stopped (via 'sudo systemctl stop &lt;name&gt;.service') before files are deployed, and started (via 'sudo systemctl start &lt;name&gt;.service') before launch/attach, so the debuggee isn't fought over by a supervisor restarting it underneath the debugger. Requires the configured user to be able to run systemctl on this unit via sudo. Leave blank to disable." />
          </StackPanel>

          <StackPanel Style="{StaticResource FieldRowStyle}">
            <Label Content=".NET executable" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding RemoteDotNetPath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldTextBoxStyle}" />
            <TextBlock Style="{StaticResource FieldHelpTextStyle}"
                       Text="Path of the .NET executable on remote machine. (Samples: `dotnet`, `~/.dotnet/dotnet`)" />
          </StackPanel>

          <StackPanel Style="{StaticResource FieldRowStyle}">
            <Label Content="Visual Studio Debugger Path" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding RemoteVsDbgRootPath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldTextBoxStyle}" />
            <TextBlock Style="{StaticResource FieldHelpTextStyle}"
                       Text="Root folder of Visual Studio Debugger. (Samples: `~/.vs-debugger/`, `~/.vsdbg`)" />
          </StackPanel>

          <StackPanel Style="{StaticResource FieldRowStyle}">
            <CheckBox Content="Use Sudo for Debugger"
                       IsChecked="{Binding UseSudoForDebugger}"
                       Style="{StaticResource FieldCheckBoxStyle}" />
            <TextBlock Style="{StaticResource FieldHelpTextStyle}"
                       Text="Launch VSDBG on the remote machine via the command in 'Sudo Command'. Use this when the debuggee process runs with elevated or ambient capabilities that the debugger must also hold in order to attach (i.e. ptrace requires the tracer's capabilities to be a superset of the tracee's). The configured user must be able to run the debugger path via sudo (passwordless or otherwise)." />
          </StackPanel>

          <StackPanel Style="{StaticResource FieldRowStyle}">
            <Label Content="Sudo Command" Style="{StaticResource FieldLabelStyle}" />
            <TextBox Text="{Binding SudoCommand, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldTextBoxStyle}" />
            <TextBlock Style="{StaticResource FieldHelpTextStyle}"
                       Text="Command used to elevate the debugger when 'Use Sudo for Debugger' is enabled. '-E' preserves the environment (VSDBG needs it); '-n' fails fast instead of prompting for a password. (Default: `sudo -n -E`)" />
          </StackPanel>

          <StackPanel Style="{StaticResource FieldRowStyle}">
            <CheckBox Content="Use Self-Contained Deployment"
                       IsChecked="{Binding UseSelfContainedDeployment}"
                       Style="{StaticResource FieldCheckBoxStyle}" />
            <TextBlock Style="{StaticResource FieldHelpTextStyle}"
                       Text="Launch the deployed program directly as a native executable instead of via 'dotnet &lt;assembly&gt;.dll'. Use this when the project is published self-contained (or AOT) for the remote machine's runtime identifier." />
          </StackPanel>

        </StackPanel>
      </GroupBox>
    </StackPanel>
  </ScrollViewer>
</UserControl>
